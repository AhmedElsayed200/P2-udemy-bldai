import CoursesSection from "../Courses Section/CoursesSection";

/* MainPage component shows all Udemy page section */
const MainPage = () => {
  return (
    <div>
      {/* courses section */}
      <CoursesSection />
    </div>
  );
}

export default MainPage;
